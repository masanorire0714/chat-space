%body
  .Side-bar
    .Side-bar__user
      .top-items
        %p.top-items__name 
        = current_user.name
      .side-icon
        = link_to new_group_path, class: "side-icon__btn" do
          = icon('far', 'edit')
        = link_to edit_user_path(current_user) do
          = icon('fa',  'cog') 
    .group-list
      - current_user.groups.each do |group|
        .side-list
          = link_to '#' do
            .side-list__name
              techcamp
            .side-list__information
              まだメッセージはありません
    